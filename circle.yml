machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/bundle"
  override:
    - docker-compose -f docker-compose.ci.yml pull
    - docker-compose -f docker-compose.ci.yml build test
    - docker-compose -f docker-compose.ci.yml run test bundle install

database:
  override:
    - echo "Skipping database"

test:
  override:
    - bin/cibuild
